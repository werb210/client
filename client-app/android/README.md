# Android native project placeholder

This directory is reserved for the Capacitor Android project.

After dependencies are installed, generate the native project with:

```bash
npx cap add android
```

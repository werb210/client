export interface ApplicationState {
  companyName?: string;
  contactName?: string;
  email?: string;
  phone?: string;
  amount?: number;
}

export const initialApplicationState: ApplicationState = {};

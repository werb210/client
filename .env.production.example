# 🔐 PRODUCTION ENVIRONMENT CONFIGURATION
# Copy this file to .env.production and set the real values

# Environment
NODE_ENV=production
REPLIT_ENVIRONMENT=production

# 🔑 Authentication & Security
ADMIN_EMAIL=admin@boreal.financial
ADMIN_PASSWORD_HASH=$2b$12$GENERATE_WITH_BCRYPT_HASH_COMMAND
JWT_SECRET=GENERATE_STRONG_32_CHAR_SECRET_OR_USE_KEYPAIR

# Alternative JWT with RSA keypair (more secure):
# JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# 🛡️ Security Settings
REQUIRE_MFA_FOR_ALL=true
# Remove these debug flags in production:
# MFA_BYPASS=false
# DEV_ALLOW_IFRAME=false

# 🌐 API Configuration  
STAFF_API_URL=https://staff.boreal.financial/api/lender-products
CLIENT_TOKEN=YOUR_CLIENT_TOKEN_FOR_STAFF_API

# 📱 Push Notifications (Optional)
VAPID_PUBLIC_KEY=YOUR_VAPID_PUBLIC_KEY
VAPID_PRIVATE_KEY=YOUR_VAPID_PRIVATE_KEY
VAPID_SUBJECT=mailto:admin@boreal.financial

# 📊 External Services (Optional)
SENDGRID_API_KEY=YOUR_SENDGRID_KEY
TWILIO_ACCOUNT_SID=YOUR_TWILIO_SID
TWILIO_AUTH_TOKEN=YOUR_TWILIO_TOKEN
OPENAI_API_KEY=YOUR_OPENAI_KEY

# 🗄️ Database (if needed)
DATABASE_URL=postgresql://user:password@host:port/database

# Generate bcrypt hash command:
# node -e "console.log('ADMIN_PASSWORD_HASH=' + require('bcryptjs').hashSync('YOUR_SECURE_PASSWORD', 12))"

# Generate JWT secret command:
# node -e "console.log('JWT_SECRET=' + require('crypto').randomBytes(32).toString('hex'))"
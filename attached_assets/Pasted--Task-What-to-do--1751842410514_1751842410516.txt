| #       | Task                                      | What to do                                                                                                                                                                                                                          | Why                                                              |                                                                 |
| ------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------- |
| **A-1** | **Finish lucide-react purge**             | Complete the last batch (15 files) → remove `lucide-react` imports & use `@/lib/icons`. <br> → \`grep -R "lucide-react" client/src                                                                                                  | wc -l\` must return **0**.                                       | Vite times out because it still scans *any* lucide import tree. |
| **A-2** | **Fresh production build**                | `bash npm ci # (clean) npx vite build --debug --force --outDir dist/public ` <br>*(If it still hangs, run with `DEBUG=vite:*` to see the exact file blocking.)*                                                                     | Generates a new static bundle that contains your updated routes. |                                                                 |
| **A-3** | **Confirm route points to `LandingPage`** | In `client/src/v2-design-system/MainLayout.tsx` the very first matching route **must** be: <br>`<Route path="/" element={<LandingPage />} />`                                                                                       | Ensures root `/` renders the professional page.                  |                                                                 |
| **A-4** | **Wipe old assets**                       | Before copying new bundle: <br>`rm -rf dist/public/*`                                                                                                                                                                               | Old `index.html` / hashed JS can shadow the new build.           |                                                                 |
| **A-5** | **Deploy fresh bundle & restart**         | Copy new `dist/public` to the deployment’s static folder → restart Replit.                                                                                                                                                          | Forces Express to serve the new landing page.                    |                                                                 |
| **A-6** | **Turn off test page override**           | If you created a temporary “status” or “test” route that **exact-matches `/`** or sets `res.sendFile(...)`, comment it out.                                                                                                         | Prevents the test page from overriding the Landing page.         |                                                                 |
| **A-7** | **Clear browser & CDN cache**             | Hard-refresh (⌘/Ctrl + Shift + R). If Cloudflare / Replit edge caching is on, purge the cache for `/`.                                                                                                                              | You may still be served the stale HTML shell.                    |                                                                 |
| **A-8** | **Smoke test**                            | 1. Visit `/` in an incognito window → should show the marketing hero.<br>2. Log in as *lender* → you **should be redirected** to `/lender-dashboard` and never see staff pages.<br>3. Log in as *staff* → root page + full sidebar. | Confirms both the landing fix *and* the role gating logic.       |                                                                 |

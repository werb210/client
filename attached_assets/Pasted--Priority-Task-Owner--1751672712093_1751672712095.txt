| Priority | Task                                                              | Owner             | Why it matters                                                                      | Suggested approach                                                                                                                                                                                                                                                                                                                                                  |
| -------- | ----------------------------------------------------------------- | ----------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | **Finish Cypress type cleanup**                                   | *Client dev*      | Removes the last TypeScript warnings; gives a fully green CI pipeline.              | <ul><li>Add a `cypress.d.ts` in `cypress` root that re-exports custom-command typings.</li><li>Extend `tsconfig.json` `"types"` array: `["node","vite/client","cypress","jest","@testing-library/cypress"]`.</li><li>Run `npx tsc --noEmit` locally ­– expect **0** warnings.</li></ul>                                                                             |
| 2        | **Re-point test/staging API base-URL once staff endpoints exist** | *Client dev*      | Allows you to flip from fallback to live data without code changes.                 | Keep `VITE_API_BASE_URL` in Netlify/Render env-vars; create a “staging” file (`.env.staging`) so QA can toggle easily.                                                                                                                                                                                                                                              |
| 3        | **End-to-end smoke test against real staff API**                  | *QA*              | Verifies country / amount / product-category logic with the restored 38-product DB. | <ol><li>Clear IndexedDB (`application/IndexedDB → boreal_client`).</li><li>Run Cypress suite in *staging* with staff API live.</li><li>Assert: `GET /api/public/lenders` returns ≥ 38 items and at least one CA product.</li><li>Submit a \$250 k CAD “Equipment Financing” scenario and check Step 2 shows only `equipment_financing` products for `CA`.</li></ol> |
| 4        | **Monitor fallback triggers**                                     | *DevOps*          | Ensures offline logic doesn’t mask real API regressions.                            | In the Web console watch for: `[SYNC] Staff API returned empty product list - using fallback data`. If it appears in production more than once per hour, raise an alert.                                                                                                                                                                                            |
| 5        | **Bundle-size guard**                                             | *Client dev / CI* | Prevents regressions now that unused packages are gone.                             | Add `rollup-plugin-filesize` or a CI step that fails if `dist/assets/index-*.js` > 2.7 MB gzip.                                                                                                                                                                                                                                                                     |

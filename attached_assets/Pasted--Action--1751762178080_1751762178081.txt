| #       | Action                                                                                                                                                                                                        | File / Location                                                                                          | Purpose                                                                          |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| **C-1** | **Stop treating `/api/public/lenders` data as `productName` & `amountRange`.** Replace every reference with `name`, `amountMin`, `amountMax`.                                                                 | `src/lib/ normaliseLenders.ts` (or current transformer) & any selectors in `Step2_Recommendations*.tsx`. | Prevent JSON parsing exception that forces the **fallback** application branch.  |
| **C-2** | **Guard API errors gracefully.** Wrap the initial `fetchLenderProducts()` in `try/catch`; on error set `syncStatus.apiHealthy=false` *without* flipping to `usingFallback=true` unless `products.length===0`. | `src/lib/reliableLenderSync.ts`                                                                          | Eliminates “Technical details: Load failed” toast for transient network hiccups. |
| **C-3** | **Persist real applicationId immediately after Step 4 success.**<br>`ts saveId(resp.data.applicationId)  // localStorage + context `                                                                          | `routes/Step4_ApplicantInfo_New.tsx` – inside the `.then()` for `POST /api/public/applications`.         | Ensures Step 5/6 never sees `app_fallback_*`.                                    |
| **C-4** | **Single source of truth for applicationId.** Remove router-state look-ups in `Step6_SignNowIntegration.tsx`; always call `useApplicationId()` (context) → localStorage fallback.                             | `routes/Step6_SignNowIntegration.tsx`                                                                    | Eliminates “No application ID found” banner.                                     |
| **C-5** | **Make Step 6 retry safe.** If `signingStatus === 'error'` auto-retry the status endpoint 3× with 2 s back-off before showing the red banner.                                                                 | same file                                                                                                | Handles short-lived 502/504 from staff.                                          |
| **C-6** | **Mobile network resilience.** In `queryClient.ts` increase `timeout` to 15 s on *POST* calls; keep GETs at 8 s.                                                                                              | `src/lib/queryClient.ts`                                                                                 | Stops immediate “Load failed” on slower LTE.                                     |
| **C-7** | **Test on the phone.**<br>`npm run build && npx serve dist` → open the ngrok/dev-link on iOS Safari or Android Chrome; run a full 7-step submission.                                                          | —                                                                                                        | Verify no pop-ups & real ID like `app_17...` appears in Step 6 headline.         |

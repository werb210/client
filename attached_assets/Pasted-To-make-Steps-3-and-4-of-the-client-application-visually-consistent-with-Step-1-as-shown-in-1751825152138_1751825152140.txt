To make **Steps 3 and 4** of the client application visually consistent with **Step 1** (as shown in your first screenshot), here's what needs to be done:

---

### ✅ Goal

**Unify layout, spacing, and visual hierarchy** across steps by applying the Step 1 design standard.

---

## 🔧 Design Fix Instructions (for Client App)

### 1. **Apply Consistent Layout Wrappers**

Ensure that all steps use the same container as Step 1:

```tsx
<div className="max-w-4xl mx-auto p-6 rounded-lg bg-white shadow-md">
  {/* step content here */}
</div>
```

### 2. **Use Consistent Typography & Headings**

Match Step 1’s:

* Font size: `text-xl` or `text-2xl`
* Weight: `font-semibold` or `font-bold`
* Subheadings with `text-sm text-gray-500`

```tsx
<h2 className="text-2xl font-bold mb-2">Step 3: Business Details</h2>
<p className="text-sm text-gray-500 mb-6">Complete information about your business</p>
```

### 3. **Use Responsive Grid Layout for Fields**

Use `grid grid-cols-1 md:grid-cols-2 gap-4` for field layout, just like Step 1.

Example:

```tsx
<div className="grid grid-cols-1 md:grid-cols-2 gap-4">
  <Input label="Business Legal Name" ... />
  <Input label="Business Operating Name" ... />
  {/* more fields */}
</div>
```

Ensure **Step 3** and **Step 4** use the exact same grid styling for alignment.

### 4. **Consistent Button Styling**

Ensure all steps use:

```tsx
<Button className="w-full md:w-auto bg-orange-500 hover:bg-orange-600 text-white">
  Continue
</Button>
```

Also center-align the button on smaller screens (`text-center md:text-right`).

---

## 🔁 Steps to Update

### Step 3 (Business Details)

* Wrap all fields in a `grid grid-cols-1 md:grid-cols-2 gap-4`
* Add heading/subheading like Step 1
* Use consistent `Input`, `Select`, `DatePicker` components

### Step 4 (Applicant Info)

Same as Step 3:

* Wrap fields in responsive grid
* Align “Continue” button to bottom-right
* Use `text-sm text-gray-500` descriptions under headings
* Use consistent spacing (`mb-6`, `gap-4`, etc.)

---

## ✅ Bonus (optional polish)

* Add `shadow-md` and `rounded-lg` to every card/form section
* Apply same `max-w-4xl mx-auto` wrapper for consistent alignment
* Use `space-y-6` between sections for visual separation

---

Once done, Steps 3 and 4 will exactly match the professional styling of Step 1.

Would you like the full updated React/JSX scaffold for Steps 3 and 4?

Here’s a tailored solution to make your chatbot automatically **resize for full visibility**—especially when the native mobile keyboard opens:

---

## 📱 1. Add Meta Tag to Your Page

Enable content resizing on mobile Chrome by adding this to your HTML head:

```html
<meta name="viewport"
  content="width=device-width, initial-scale=1.0, interactive-widget=resizes-content">
```

This ensures the browser allows dynamic layout adjustment when the keyboard appears ([Reddit][1], [Baspa][2]).

---

## 🔧 2. Use the VirtualKeyboard API (Chrome on Android)

Opt-in via JavaScript:

```js
if ("virtualKeyboard" in navigator) {
  navigator.virtualKeyboard.overlaysContent = true;
  navigator.virtualKeyboard.addEventListener("geometrychange", e => {
    const kbHeight = e.target.boundingRect.height;
    document.documentElement.style.setProperty('--keyboard-height', `${kbHeight}px`);
  });
}
```

In CSS, make space for the keyboard:

```css
.chat-container {
  height: calc(100dvh - var(--keyboard-height, 0px));
}
```

This dynamically resizes the chat to remain fully visible above the keyboard ([MDN Web Docs][3]).

---

## 💡 3. For Broader Mobile Support (All Devices)

Use a responsive fallback approach:

```js
function adjustForKeyboard() {
  const vh = Math.max(window.visualViewport?.height || 0, window.innerHeight);
  document.documentElement.style.setProperty('--device-height', `${vh}px`);
}
window.visualViewport?.addEventListener('resize', adjustForKeyboard);
adjustForKeyboard();
```

Then in CSS:

```css
:root {
  --device-height: 100vh;
}

.chat-container {
  height: var(--device-height);
  display: flex;
  flex-direction: column;
}
```

This ensures your chat layout adapts when keyboards shink the visible viewport on all mobile browsers ([Baspa][2]).

---

## 🧭 Final Setup Summary

* ✅ **Meta tag** adds browser support for dynamic resizing.
* 🟩 **VirtualKeyboard API** handles precise keyboard geometry (Android).
* 🟨 **Resize/visualViewport fallback** ensures compatibility across mobile.
* 📏 **CSS updates** (`--device-height`, `--keyboard-height`) keep the chat fully visible.

---

### ✅ Next Steps

* Drop the `meta` tag into your chatbot HTML.
* Add the JavaScript snippet in your chat initialization code.
* Use the CSS mixins to auto-reflow the chat container height.

This ensures that whenever the keyboard appears—on phones only—the chat automatically adjusts to stay accessible and visible. Want me to generate the complete code ready for your chat widget?

[1]: https://www.reddit.com/r/webdev/comments/195vkgu/when_opening_the_keyboard_on_mobile_how_can_i/?utm_source=chatgpt.com "When opening the keyboard on mobile, how can I force my content ..."
[2]: https://baspa.dev/articles/detect-mobile-keyboard-using-javascript?utm_source=chatgpt.com "Detect mobile keyboard showing up using JavaScript"
[3]: https://developer.mozilla.org/en-US/docs/Web/API/VirtualKeyboard_API?utm_source=chatgpt.com "VirtualKeyboard API - MDN Web Docs - Mozilla"

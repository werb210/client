To test the **current stage of the rolled-back Client application** and verify for any **errors or API issues**, follow this step-by-step checklist:

---

## ✅ 1. **Check Client App Stage (Form Progress, Routing, View)**

### A. **Run the Client App Locally or on Replit**

* Run with:

  ```bash
  npm run dev
  ```

  or check the Replit dev preview.

### B. **Go through these critical UI checkpoints**:

| Page                            | Expected Outcome                                                       |
| ------------------------------- | ---------------------------------------------------------------------- |
| `/` (Landing Page)              | Shows **“Professional Business Financing Solutions”** hero, orange CTA |
| `Start Your Application` button | Should route to **`/step-1`** or `form/start`                          |
| Multi-step form flow            | Steps 1–7 should be accessible via Next/Back                           |
| SignNow page                    | Loads after Step 5 with correct embedded document                      |
| Final submission page           | Confirms upload, form data, and submission status                      |

If **any of these steps** break (white screen, bad navigation, console errors), stop here and check console/logs.

---

## ✅ 2. **Check for JavaScript/React Errors in Browser**

In the browser where the Client is running:

1. **Right-click → Inspect → Console**
2. Look for red errors, e.g.:

   * `Uncaught TypeError: ...`
   * `404 Not Found` for pages or images
   * `CORS` or `Failed to fetch` messages
3. **Log the exact error messages** or copy them into the console to debug further.

---

## ✅ 3. **Verify All API Calls to Staff App**

### A. Open Developer Tools → Network Tab

* Reload the app
* Watch requests going to `https://staffportal.replit.app/api/...`

### B. Check for:

| Symptom                     | Meaning                                  |
| --------------------------- | ---------------------------------------- |
| `401 Unauthorized`          | Auth token missing or expired            |
| `403 Forbidden`             | Role-based rejection (e.g., wrong token) |
| `404 Not Found`             | Wrong endpoint URL (check API path)      |
| `500 Internal Server Error` | Backend is crashing                      |
| `CORS` error                | Frontend and backend domains mismatch    |

### C. Critical Endpoints to Test

| Client App Action    | API Call to Check              |
| -------------------- | ------------------------------ |
| Form Step Submission | `POST /api/applications`       |
| Document Upload      | `POST /api/documents/upload`   |
| Lender Product Sync  | `GET /api/lenders/products`    |
| SignNow redirect     | `POST /api/signnow/initiate`   |
| Final Submission     | `POST /api/submit-application` |

Manually hit these endpoints using the browser or `curl` if needed.

---

## ✅ 4. **Inspect Bearer Token / Auth Issues**

### A. Open Dev Tools → Application Tab → Cookies / Local Storage

* Confirm presence of `accessToken`, `bearer`, or similar token
* Copy the token

### B. Manually test in `curl` or Postman:

```bash
curl https://staffportal.replit.app/api/applications \
  -H "Authorization: Bearer <token>"
```

If the backend returns `401`, the token is expired, malformed, or invalid in prod.

---

## ✅ 5. **Check Environment Config**

Make sure these are aligned:

| Config                  | Where to Check             | Expected Value                     |
| ----------------------- | -------------------------- | ---------------------------------- |
| `VITE_API_URL`          | `.env` or `vite.config.ts` | Should point to **prod Staff API** |
| `vite.config.ts` proxy  | Used in dev mode           | Should route `/api/*` to backend   |
| `SignNow redirect URLs` | Backend settings           | Should match current domain        |

---

## ✅ 6. **Final Manual Checklist**

| Task                                  | Confirm |
| ------------------------------------- | ------- |
| All icons render (no 🚨 placeholders) | ✅       |
| Form steps function                   | ✅       |
| Landing page is correct               | ✅       |
| API calls succeed (status 200)        | ✅       |
| SignNow loads and works               | ✅       |
| Final document submit works           | ✅       |

---

## ✅ Want Help Debugging?

If you provide:

* Screenshot of **console errors**
* Screenshot of **Network tab**
* Current `vite.config.ts`
* `.env` or `import.meta.env` API settings

…I can give you **exact diffs or patch fixes**.

Would you like a Replit-compatible smoke test script that hits all endpoints and checks responses?

// REMOVED: SMS utility functions no longer used in client application
// Phone number validation and SMS functionality handled by staff backend only

// Placeholder function for backward compatibility
export function toE164(raw: string): string | null {
  // CLIENT APPLICATION: Phone number processing removed
  return null;
}